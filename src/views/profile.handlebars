<div class="profile-container">
    <div class="profile-card">
        <h1>Conta do Usuário</h1>
        
       <div class="profile-container">
  <div class="sidebar">
    <h2>Minha Conta</h2>
    <ul>
      <li class="tab active" data-tab="dados">Dados Pessoais</li>
      <li class="tab" data-tab="enderecos">Endereços</li>
      <li class="tab" data-tab="pedidos">Pedidos</li>
      <li class="tab" data-tab="cartoes">Cartões</li>
      <li class="tab" data-tab="autenticacao">Autenticação</li>
      <li class="tab" data-tab="favoritos">Favoritos</li>
      <li class="tab" data-tab="pedidos-antigos">Pedidos Antigos</li>
      <li><a href="/logout" class="logout-link">Sair</a></li>
    </ul>
  </div>

  <div class="content">
    <!-- DADOS PESSOAIS -->
    <div class="tab-content active" id="dados">
      <h3>Dados Pessoais</h3>
      <p><strong>Nome:</strong> {{user.first_name}}</p>
      <p><strong>Sobrenome:</strong> {{user.last_name}}</p>
      <p><strong>Email:</strong> {{user.email}}</p>
      <p><strong>CPF:</strong> {{user.cpf}}</p>
      <p><strong>Gênero:</strong> {{user.genero}}</p>
      <p><strong>Data de nascimento:</strong> {{user.dataNascimento}}</p>
      <p><strong>Telefone:</strong> {{user.telefone}}</p>
      <button class="edit-button">Editar</button>
    </div>

    <!-- Outras abas vazias por enquanto -->
    <div class="tab-content" id="enderecos"><h3>Endereços</h3></div>
    <div class="tab-content" id="pedidos"><h3>Pedidos</h3></div>
    <div class="tab-content" id="cartoes"><h3>Cartões</h3></div>
    <div class="tab-content" id="autenticacao"><h3>Autenticação</h3></div>
    <div class="tab-content" id="favoritos"><h3>Favoritos</h3></div>
    <div class="tab-content" id="pedidos-antigos"><h3>Pedidos Antigos</h3></div>
  </div>
</div>

<style>
  .profile-container {
    display: flex;
    max-width: 1000px;
    margin: 40px auto;
    font-family: sans-serif;
  }

  .sidebar {
    width: 220px;
    background-color: #f3f3f3;
    padding: 20px;
    border-right: 1px solid #ccc;
    border-radius: 8px 0 0 8px;
  }

  .sidebar h2 {
    font-size: 20px;
    margin-bottom: 15px;
  }

  .sidebar ul {
    list-style: none;
    padding: 0;
  }

  .sidebar ul li, .sidebar ul li a {
    padding: 10px;
    cursor: pointer;
    color: #333;
    text-decoration: none;
    display: block;
    transition: background 0.3s;
  }

  .sidebar ul li:hover, .sidebar ul li.active {
    background-color: #ddd;
    border-radius: 4px;
  }

  .content {
    flex-grow: 1;
    padding: 20px;
    background-color: #fff;
    border-radius: 0 8px 8px 0;
  }

  .tab-content {
    display: none;
  }

  .tab-content.active {
    display: block;
  }

  .edit-button {
    margin-top: 10px;
    padding: 8px 12px;
    background-color: #820082;
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
  }

  .edit-button:hover {
    background-color: #a000a0;
  }
  #logged-in-info {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 5px; /* Espaço entre o texto e o botão */
}

</style>
