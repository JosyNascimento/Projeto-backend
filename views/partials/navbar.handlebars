<nav class="navbar navbar-expand-lg custom-navbar">
    <div class="container">
        <a href="/" class="navbar-brand">Keraprof</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <div class="d-flex flex-column flex-lg-row justify-content-between align-items-start align-items-lg-center w-100 gap-3">
                <div class="d-flex flex-column flex-lg-row align-items-start align-items-lg-center gap-3 w-100">
                    <div class="search-container w-100 w-lg-auto">
                        <input type="text" placeholder="Search" class="search-input">
                        <i class="fas fa-search search-icon"></i>
                    </div>
                    <ul class="navbar-nav d-flex flex-column flex-lg-row align-items-start align-items-lg-center gap-2">
                        <li class="nav-item">
                            <a href="/" class="nav-link {{#if activeList}}active{{/if}}">Ver Loja</a>
                        </li>
                        <li class="nav-item">
                            <a href="/list" class="nav-link {{#if activeList}}active{{/if}}">User List</a>
                        </li>
                    </ul>
                </div>
                <div class="d-flex flex-column flex-lg-row align-items-start align-items-lg-center gap-3">
                    <div class="cart-fav-icons">
                        <a href="/cart" class="cart-icon">
                            <i class="fas fa-shopping-cart"></i>
                        </a>
                        <a href="/favorites" class="fav-icon">
                            <i class="fas fa-heart"></i>
                        </a>
                    </div>
                    <div class="user-link">
                        {{#if user}}
                            <a href="/perfil" class="user-avatar">
                                <i class="fas fa-user-circle fa-1x"></i>
                            </a>
                            <div class="user-message d-flex flex-row align-items-center gap-2">
                                <span id="welcome-message">Bem-vindo, {{user.first_name}}</span>
                    
                                <button id="logoutBtn" class="btn btn-sm btn-outline-secondary">Sair</button>
                            </div>
                        {{else}}
                            <a href="/login" class="user-avatar">
                                <i class="fas fa-user-circle fa-1x"></i>
                            </a>
                            <div class="user-message d-flex flex-row align-items-center gap-2">
                                <span id="login-register-message" class="d-block">
                                    Olá, faça seu <a href="/login">login</a> ou <a href="/register">cadastre-se</a>
                                </span>
                            </div>
                        {{/if}}
                    </div>
                </div>
            </div>
        </div>
    </div>
</nav>
<script>
    document.addEventListener('DOMContentLoaded', () => {
        const logoutBtn = document.getElementById('logoutBtn');
        if (logoutBtn) {
            logoutBtn.addEventListener('click', async () => {
                try {
                    const response = await fetch('/api/auth/logout', { method: 'POST' });
                    if (response.ok) {
                        window.location.reload();
                    } else {
                        console.error('Erro ao fazer logout');
                    }
                } catch (error) {
                    console.error('Erro ao fazer logout:', error);
                }
            });
        }
    });
</script>
</body>
</html>

 <!--<a href="/perfil" class="btn btn-sm btn-outline-danger">Perfil</a>>->